[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codesearch"
version = "0.1.0"
description = "Hybrid Semantic Code Search Engine"
requires-python = ">=3.11"
